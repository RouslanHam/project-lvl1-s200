import gameLogic from '../index';

const gameIntro = '';

const tasks = () => {
  const result = null;
  return result;
};


export default () => {
  const result = gameLogic(gameIntro, tasks);
  return result;
};
